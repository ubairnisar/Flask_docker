name: CI/CD with Docker

on:
  push:
    branches:
      - main # Adjust the branch name as needed

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker buildx create --use
          docker buildx build --platform linux/amd64 -t ubairnisar/mlops:latest .
        # Note: We removed the --output flag since it's not necessary.

      - name: Push Docker image to Docker Hub
        run: |
          docker push ubairnisar/mlops:latest

      - name: Run Docker container
        run: |
          docker run -d -p 4000:8080 ubairnisar/mlops:latest
